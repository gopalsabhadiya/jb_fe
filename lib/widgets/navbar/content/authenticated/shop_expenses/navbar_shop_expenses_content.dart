import 'package:flutter/cupertino.dart';

class NavbarShopExpensesContent extends StatelessWidget {
  const NavbarShopExpensesContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [Container()],
    );
  }
}
